<div class="absolute bottom-12 left-1/2 -translate-x-1/2 w-[90%] max-w-2xl flex flex-col gap-12">
  <div class="flex flex-col justify-start gap-4">
    <%= render partial: "messages/message", collection: @messages %>
    <%= render LlmPromptComponent.new(message: @message) %>
  </div>
</div>
