<!DOCTYPE html>
<html data-theme="dark">
  <head>
    <title>T3Clone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-white dark:bg-gray-900">
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
        <span class="block sm:inline"><%= notice %></span>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
        <span class="block sm:inline"><%= alert %></span>
      </div>
    <% end %>
    <main class="text-black dark:text-white flex flex-col md:flex-row md:items-center h-screen" data-controller="scroll">
      <div class="block md:hidden fixed top-8 left-8 rounded-full bg-gray-900 border border-gray-300 dark-border-gray-600 p-2 w-12 h-12">
        <%= link_to chats_path, class:"w-full h-full flex items-center justify-center" do %>
          <%= heroicon "arrow-uturn-left", options: { class: "w-6 h-6 text-white" } %>
        <% end %>
      </div>
      <nav class="hidden md:block w-64 h-screen bg-gray-100 dark:bg-gray-800 overflow-hidden">
        <div class="flex items-center gap-4 justify-between px-4 py-3 border-b border-gray-200 dark:border-gray-700">
          <!-- Circle user icon -->
          <%= link_to "New Chat", chats_path, class: "block flex-1 text-center px-4 py-2 rounded border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-900 dark:text-white font-medium hover:bg-gray-50 dark:hover:bg-gray-600" %>
          <%= link_to chats_path do %>
            <div class="flex items-center justify-center w-8 h-8 rounded-full bg-gray-300 dark:bg-gray-600 text-gray-700 dark:text-gray-200 font-bold">
              <%= Current.user.email_address.split("@").first.first.upcase %>
            </div>
          <% end %>
        </div>
        <div class="flex-1 px-4 py-4 overflow-y-auto">
          <ul class="space-y-3">
            <li>
            </li>
            <% Current.user.chats.ordered.limit(10).each do |chat| %>
              <li>
                <%= link_to chat.title.presence || "chat #{chat.id}", chat_path(chat), class: "block w-full text-left px-4 py-2 rounded border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-900 dark:text-white hover:bg-gray-50 dark:hover:bg-gray-600" %>
              </li>
            <% end %>
          </ul>
        </div>
      </nav>
      <section class="flex flex-1 flex-col items-center h-screen transition-all duration-300">
        <section class="flex-1 overflow-y-auto w-full " data-scroll-target="container">
          <%= yield %>
        </section>
        <footer class="w-full">
          <%= yield :prompt_region %>
        </footer>
      </section>
    </main>
  </body>
</html>
